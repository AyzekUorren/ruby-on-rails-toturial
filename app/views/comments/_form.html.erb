<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <p>
    <%= form.label :author %>
    <%= form.text_field :author %>
  </p>
  <p>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </p>

  <P>
    <%= form.submit %>
  </P>
<% end %>
